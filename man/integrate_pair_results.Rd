% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_analysis.R
\name{integrate_pair_results}
\alias{integrate_pair_results}
\title{Integrate significant gene pairs across datasets}
\usage{
integrate_pair_results(
  pair_results,
  fdr_cutoff = 0.05,
  min_datasets = 2,
  min_direction_consistency = 0.67,
  pair_col = NULL,
  gene1_col = "gene1",
  gene2_col = "gene2",
  or_col = "OR",
  p_col = "fisher_p",
  adjusted_p_col = "adjusted_p",
  canonicalize_pairs = TRUE
)
}
\arguments{
\item{pair_results}{A named list of data frames, or a named character vector
of file paths. Names are used as dataset names.}

\item{fdr_cutoff}{Adjusted p-value cutoff used to define significant pairs.}

\item{min_datasets}{Minimum number of datasets in which a pair must be
significant.}

\item{min_direction_consistency}{Minimum OR-direction consistency across
significant occurrences. A value of 1 requires all significant occurrences
to have the same direction.}

\item{pair_col}{Optional column containing a pair identifier. If \code{NULL}, the
function uses \code{gene1} and \code{gene2} when available.}

\item{gene1_col, gene2_col}{Columns containing the two genes in each pair.}

\item{or_col}{Column containing the odds ratio.}

\item{p_col}{Column containing Fisher's exact test p-value.}

\item{adjusted_p_col}{Column containing adjusted p-values. If absent, values
are calculated from \code{p_col}.}

\item{canonicalize_pairs}{If \code{TRUE}, gene-pair identifiers are sorted so
\code{A|B} and \code{B|A} are treated as the same pair.}
}
\value{
A list with \code{summary}, \code{all_pairs}, and \code{significant_pairs}.
}
\description{
Integrate significant gene pairs across datasets
}
\examples{
cohort1_pairs <- data.frame(
  gene1 = c("CD8A", "CXCL9", "MKI67"),
  gene2 = c("PDCD1", "LAG3", "TOP2A"),
  OR = c(5.2, 3.1, 0.8),
  fisher_p = c(0.0004, 0.006, 0.20),
  adjusted_p = c(0.004, 0.03, 0.50)
)
cohort2_pairs <- data.frame(
  gene1 = c("CD8A", "CXCL9", "GZMB"),
  gene2 = c("PDCD1", "LAG3", "IFNG"),
  OR = c(4.4, 2.6, 0.7),
  fisher_p = c(0.001, 0.008, 0.10),
  adjusted_p = c(0.006, 0.04, 0.30)
)
cohort3_pairs <- data.frame(
  gene1 = c("CD8A", "CXCL10", "MKI67"),
  gene2 = c("PDCD1", "TIGIT", "TOP2A"),
  OR = c(6.0, 2.2, 1.3),
  fisher_p = c(0.0002, 0.02, 0.09),
  adjusted_p = c(0.003, 0.08, 0.25)
)

integrated <- integrate_pair_results(
  list(cohort1 = cohort1_pairs, cohort2 = cohort2_pairs, cohort3 = cohort3_pairs),
  min_datasets = 2
)
integrated$summary
}
