% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{plot_pair_roc}
\alias{plot_pair_roc}
\title{Plot ROC curves for gene pairs}
\usage{
plot_pair_roc(
  result,
  gene1 = NULL,
  gene2 = NULL,
  top_n = 1,
  delta = 0.25,
  score = c("difference", "state"),
  response_col = "response",
  response_label = "response",
  use_sig_pairs = TRUE,
  direction = c("auto", "as_is"),
  log_transform = TRUE,
  file = NULL,
  width = 6,
  height = 6,
  main = NULL
)
}
\arguments{
\item{result}{Result list returned by \code{\link[=run_pair_marker_analysis]{run_pair_marker_analysis()}}.}

\item{gene1, gene2}{Optional gene names. If omitted, top pairs are selected from the result table.}

\item{top_n}{Number of top pairs to plot when \code{gene1} and \code{gene2} are not supplied.}

\item{delta}{Pairwise expression difference cutoff.}

\item{score}{Score used for ROC analysis. \code{difference} uses the continuous expression difference \code{gene1 - gene2}; \code{state} uses the 0/1 pair state.}

\item{response_col}{Clinical column containing response labels.}

\item{response_label}{Label treated as response.}

\item{use_sig_pairs}{If \code{TRUE}, use \code{result$sig_pairs} when available.}

\item{direction}{If \code{auto}, scores are reversed when the initial AUC is below 0.5.}

\item{log_transform}{If \code{TRUE}, use \code{log2(expression + 1)} before comparison.}

\item{file}{Optional output path ending in \code{.pdf}, \code{.png}, \code{.jpg}, or \code{.jpeg}.}

\item{width}{Plot width in inches when \code{file} is provided.}

\item{height}{Plot height in inches when \code{file} is provided.}

\item{main}{Plot title.}
}
\value{
A data frame with AUC values, invisibly.
}
\description{
Plot ROC curves for gene pairs
}
